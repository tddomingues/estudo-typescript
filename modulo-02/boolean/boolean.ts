//
let tarefaConcluida: boolean = true
let tarefaPendente: boolean = false
console.log(tarefaConcluida)

//
function isEven(num: number): boolean {
    return num % 2 === 0
}
console.log(`É par ? ${isEven(11)}`)
console.log(`É par ? ${isEven(2)}`)