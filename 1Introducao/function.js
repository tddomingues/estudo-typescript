"use strict";
const sum = (x, y) => {
    return x + y;
};
console.log(sum(2, 3));
const log = (msg) => {
    //void não retorna nada
    console.log(msg);
};
const mult = (x, y) => {
    return x * y;
};
